package foilfields.shrinkdevices.client;

import net.fabricmc.api.ClientModInitializer;
import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;

/**

 Main entry point for the ShrinkDevices mod on the client-side. Initializes client-specific features and components.
 */
@Environment(EnvType.CLIENT)
public class ShrinkDevicesClient implements ClientModInitializer {

    /**
     Initializes client-side features and components for the ShrinkDevices mod.
     This method is called during client initialization.
     */
    @Override
    public void onInitializeClient() {

    }
}